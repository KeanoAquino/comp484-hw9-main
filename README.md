A list of the built-in objects and methods you used
- getFullYear()
- getMonth()
- getDate()
- Number()
- Number.isNaN()
- Number.isInteger()
- toFixed()
- document.querySelector()
- textContent
- innerHTML

Your GitHub Pages link

Path to a screenshot file of the finished webpage
hw9-screenshot.png

A short reflection of 4-5 sentences that answers:

I found the math and formatting the easiest. I found the date display the most challenging. I learned that getDay() returns the day (mon-fri in int), so I had to use getDate() instead.I also learned that declaring a variable with a number value in quotations will treat it as a string, so Number() must be used to convert it to a number to make calculations. Lastly, I learned to use innerHTML when you want to add html elements, textContent will treat everything as string.